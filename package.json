{
  "name": "gem-rush",
  "version": "2.2.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "serve src/",
    "start": "serve ./www -l $PORT",
    "build": "rm -rf www/ && grunt",
    "start:build": "grunt && serve www/"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cordova-plugin-whitelist": "^1.3.4",
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "grunt": "^1.1.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-cssmin": "^3.0.0",
    "grunt-contrib-uglify": "^4.0.1",
    "grunt-replace": "^1.0.1"
  },
  "dependencies": {
    "cordova": "^9.0.0",
    "cordova-android": "^8.1.0",
    "serve": "^11.3.0"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    },
    "platforms": [
      "android"
    ]
  }
}
